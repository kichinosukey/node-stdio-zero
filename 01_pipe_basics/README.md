# 01 Pipe Basics

`stdin` から文字列を受けて、`stdout` にJSONを返す最小例です。

## 実行
```bash
echo 'hello stdio' | node learning/node-stdio-zero/01_pipe_basics/main.js
```

## 何を確認するか
- `stdin` が入力チャネル
- `stdout` が出力チャネル
- HTTPなしでも要求/応答の形を作れる
